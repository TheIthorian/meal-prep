# VITE_PUBLIC_POSTHOG_KEY=
# VITE_PUBLIC_POSTHOG_HOST=
# Omit or leave empty in dev to use the Vite proxy (UI origin → http://localhost:5001). Set a full URL to call the API directly.
# VITE_API_BASE_URL=http://localhost:5001